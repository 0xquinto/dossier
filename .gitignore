# Exa Agent per-day cost state (counts/dollars only, no PII; persists the
# per-day spend cap across CLI invocations)
.dossier-exa-cost.json

# Pipeline run outputs (large, regenerable)
research/runs/
research/latest
research/cleanup-report.txt
research/applications.md
research/applications.md.bak
research/interview-prep/story-bank.md

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.pytest_cache/

# Environment
.venv/
.env
.dev.vars
*.dev.vars
.wrangler/

# Context sync (generated)
.context-sync-*

# Internal/handoff scratch (SaaS-branch state, vault-linked notes — not for the public repo)
STATE.md
docs/plans/

# OS
.DS_Store

# Superpowers (local plans/specs)
docs/superpowers/

# Git worktrees
.worktrees/
.claude/worktrees/

# Personal data (use templates/ to create your own)
skills-inventory.md
resume.md
resume-*.md
portals.yml
voice-sample.md
negotiation-playbook.md
node_modules/
bin/dashboard

# Personal profile drafts (LinkedIn, X bio, GitHub README, talk bios, etc.)
profile/

# Personal CV exports (root-level, generated per application — never publish)
/cv*.pdf
/cv*.html

# scripter-11 fixture outputs (generated by test runs, not checked in)
tests/scripter-11/fixtures/*/phase-4-pitch/
